const form = document.queryCommandValue('#search-weather');
// const recentSearches = document.querySelector('#recent-searches')

form.addEventListener("submit", function(event){
    event.preventDefault();
})